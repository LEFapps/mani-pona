import React from 'react'
import { View, Text } from 'react-native'

export const Dashboard = ({ navigation, route }) => {
  return (
    <View>
      <Text>LoREco Dashboard</Text>
    </View>
  )
}

export default Dashboard
